import styled from 'styled-components';

export const AuthButtonsBar = styled.nav`
  display: flex;
  justify-content: center;
  align-items: center;
  background-color: ${({ theme }) => theme.backgrounds.header};
`;
